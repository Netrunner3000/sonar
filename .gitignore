venv/
.venv/
__pycache__/
.env
*.log
*.db
*.zip
output/
.DS_Store
data/state.json
data/portfolio.json
data/protocol.json
data/*.bak.*
data/*.tmp
# Runtime single-writer lock: holds a PID from whichever machine last ran the
# engine. Committing it means a clone can start life believing another SONAR is
# already polling, and silently drop to read-only.
data/engine.lock
dist.noindex/
build/
dist/
data/cache/
assets/icon.iconset/
*.spec

# Nested companion repos: agents split into their own repos
# (gitignored here until each has a GitHub remote and becomes a proper submodule)
/sonar/macro/
/sonar/playmaker/

# Written by scripts/stamp_version.py at package time: identifies a build,
# not the source. See VERSIONING.md.
_build_info.json
